services:
  app:
    build: .
    ports:
      - "127.0.0.1:4000:3000"
    volumes:
      - ./data:/app/data
    environment:
      - NODE_ENV=production
      - DATABASE_URL=/app/data/local.db
      - ORIGIN=https://corazonazul.irisvisual.com
    restart: always
